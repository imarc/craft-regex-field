{% import "_includes/forms" as forms %}

{{ forms.textField({
        id: name~'regexField',
        name: name~'[regexField]',
        value: value ? value : '',
        placeholder: field['placeholderText'] })
}}
